using System;

namespace ForLoop7
{
    class MainClass
    {
        public static void Main(string[] args)
        {
            //for (int i = 0; i <= 10; i++)
                //Console.WriteLine(i); 
            for (int b = 100; b >= 10; b++)
                Console.WriteLine(b); //бесконечный цикл
        }
    }
}
